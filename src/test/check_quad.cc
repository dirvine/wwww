/* This is free and unencumbered software released into the public domain. */

#ifdef HAVE_CONFIG_H
#include <config.h>
#endif
#define CATCH_CONFIG_MAIN
#include "catch.hpp"

#include "wwww/quad.h" /* for rdf::quad */

TEST_CASE("test_quad", "[base]") {
  // TODO
  CHECK(true);
}
